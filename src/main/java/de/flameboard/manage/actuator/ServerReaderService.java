package de.flameboard.manage.actuator;

import org.apache.log4j.Logger;

/**
 * service that is capable of calling a execute method of each service
 * which will get the information from routes push them to a equivalent
 * holder object and passes them to the dashboard controller endpoint
 *
 * @see Service1
 * @see Service1
 * @see Service1
 *
 * @author svenklemmer
 * @since 1.0.0
 */
public class ServerReaderService {
  private static final Logger LOGGER = Logger.getLogger(ServerReaderService.class);
}
